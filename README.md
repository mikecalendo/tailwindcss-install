```
rm -rf package-lock.json yarn.lock node_modules/ && npm i && npm install -D tailwindcss@latest autoprefixer@latest postcss@latest postcss-flexbugs-fixes postcss-preset-env precss --save-dev && npx tailwind init -p

rm -rf package-lock.json yarn.lock node_modules/ && yarn && yarn add -D tailwindcss@latest autoprefixer@latest postcss@latest postcss-flexbugs-fixes postcss-preset-env precss --save-dev && npx tailwind init -p
```
